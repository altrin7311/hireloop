# Shared secret between Next.js and Python microservice
AUTOMATION_API_KEY=devsecret123

# Selenium browser
HEADLESS=true

# Path where per-user stealth state JSON lives
STEALTH_STATE_DIR=./.stealth_state

# Optional: override default port (uvicorn --port still wins if passed)
PORT=8000
